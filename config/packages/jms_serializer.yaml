jms_serializer:
    metadata:
        cache: file
        debug: "%kernel.debug%"
        file_cache:
            dir: "%kernel.cache_dir%/serializer"

        directories:
            any-name:
                namespace_prefix: "App"
                path: "%kernel.project_dir%/config/serializer"